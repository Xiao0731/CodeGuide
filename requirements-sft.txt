# Cloud-only QLoRA SFT dependencies. Install against the CUDA-enabled PyTorch image.
transformers==4.53.3
peft>=0.15,<0.18
accelerate>=1.7,<2
# 0.48.0 added official CUDA 13 wheels; cloud image currently uses torch cu130.
bitsandbytes>=0.48.2,<0.49
# Fused linear cross-entropy avoids materializing the full 8K x vocabulary logits.
liger-kernel==0.8.0
datasets>=3.6,<5
PyYAML>=6.0,<7
safetensors>=0.5,<1
